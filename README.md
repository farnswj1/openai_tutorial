# OpenAI Tutorial
This is an OpenAI tutorial.

# Setup
In the `app` directory, create an `.env` file with the following:
```
OPENAI_API_KEY=secret
```

Create a virtual environment using pipenv and run `pipenv run python main.py`.
